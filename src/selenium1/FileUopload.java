package selenium1;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;

public class FileUopload {

	public static void main(String[] args) {
		/*System.setProperty("webdriver.chrome.driver", "C:\\Users\\Akshay\\Desktop\\chromedriver.exe");

		WebDriver driver = new ChromeDriver();
		driver.manage().window().maximize();
		driver.get("  "); //url
	WebElement upload = driver.findElement(By.xpath(xpathExpression));//will take the xpath of upload web element
		
             upload.sendKeys("FileLocation");//will give the location of file
             //file will upload*/
		//to perform this actions we need "Input" tagname if its not present then we cannot perform this operation
	}

}
